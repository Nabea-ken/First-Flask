# integrating flask with bootstrap   API's and Sass/Scss

Building a responsive news API application using flask and integrating bootstrap into it

## 1. Create a virtual environment

    python -m venv Flash_Task
    Flash_Task\Scripts\activate
    pip list
    pip3 install flask requests
    pip freeze > requirements.txt
    deactivate

 **reuse the login later:
    python -m venv venv
    source venv/bin/activate   # or venv\Scripts\activate on Windows
    pip install -r requirements.txt

## 2. Project structure and setup

 **static folder-JS,CSS styling files
 **templates-HTML

 *live sass compiler

## 3. Create an API KEY

    News API-newsapi.org
    create an account
    get key

## 4. Adding bootstrap scripts

## 5. Loading Articles with our API

## 6. Filter Articles

## 7. Bootstrap Navbar

## 8. Bootstrap Form

## 9. Custom Sass/Scss styling

- install bootstrap using yarn
    yarn install
    yarn init -y -> create lock
    yarn add bootstrap
       node-modules folder

## 10. Deployment
